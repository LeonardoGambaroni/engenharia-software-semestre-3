class Veiculo {
  String renavam;
  String chassi;
  String? marca;
  int? anoFabricacao;
  
  Veiculo(this.renavam,this.chassi);
  
  bool ehIgual(Veiculo outro){
    if(this.renavam == outro.renavam && this.chassi == outro.chassi){
      return true;
    }
    return false;
  }
  
  @override
  bool operator ==(Object other) {
      if (identical(this, other) ||
      (other is Veiculo && this.renavam == other.renavam && this.chassi ==          other.chassi)){
          return true;
      }
      return false;
  }
}

void main(){
  //quando instanciamos classes nossas,
  //por padrao, cada instancia ocupara lugares diferentes de memoria,
  //mesmo que os dados sejam identicos.
  //Veiculo v1 = Veiculo('123','ABC');
  Veiculo v1 = Veiculo('456','BCD');
  v1.marca = 'Honda';
  Veiculo v2 = Veiculo('456','BCD');
  v2.marca = 'Toyota';
  //comparando igualdade (e isso é subjetivo no Dart)
  //por padrão voce está comparando enderecos de alocação.
  //CUIDADO: retire o override para comparar endereços
  if(v1 == v2){
    print('Mesmo endereco.');
  }
  else{
    print('Enderecos diferentes.');  
  }
  
  if(v1.ehIgual(v2)){
    print('Mesmos Veiculos de acordo com o metodo equals.');
  }
  else {
    print('Veiculos diferentes de acordo com o metodo equals.');
  }
  
  //DEPOIS DE SOBRESCREVER o operador ==
  if(v1 == v2){
    print('Mesmos veículos (em conteudo).');
  }
  else{
    print('Veiculos diferentes (em conteudo).');
  }
}

/*void main(){
  //entendendo comparacoes 
  //String interning
  String a = 'oi';
  String b = 'oi';
  //funcao identical
  //compara referencias de memoria de duas instancias
  bool mesmaRef = identical(a,b);
  if(mesmaRef){
    print('Strings iguais e mesma referencia');
  }
  else {
    print('Strings diferentes');
  }
}*/

